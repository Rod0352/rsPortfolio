import { Box, Typography } from "@mui/material";
import React from "react";

export const Projects = () => {
  return (
    <Box>
      <Typography variant="h3">Projects Test</Typography>
    </Box>
  );
};
